const Job = require("../models/Job");

const getAllJobs = async (req, res) => {
  try {
    // get all jobs + populate poster info
    // return 200 with jobs
  } catch (error) {
    // return 500 error
  }
};

module.exports = { getAllJobs };
